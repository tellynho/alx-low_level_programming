#include <stdio.h>

/**
 * main - prints the phrase with proper gramar,
 * but the outcome is a piece of art,
 * Return: o if exited properly, non-zero otherwise
 */
int main(void)
{
       printf("with proper gramar,but the outcome is a piece of art,\n");
       return (0);
}       
